apiVersion: apps/v1
kind: Deployment
metadata:
  name: mongo
  labels:
    app: mongo
spec:
  replicas:
  selector:
    matchLabels:
  template:
    metadata:
      labels:
    spec:
      containers:
        - name:
          image:
          ports:

---
apiVersion: v1
kind: Service
metadata:
  name: mongo
spec:
  type:
  selector:
  ports:
    - protocol:
      targetPort:
      port:
