apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  labels:
    app: backend
spec:
  replicas:
  selector:
    matchLabels:
  template:
    metadata:
      labels:
    spec:
      containers:
        - name:
          image:
          ports:

---
apiVersion: v1
kind: Service
metadata:
  name: backend
spec:
  type:
  selector:
  ports:
    - protocol:
      targetPort:
      port:
      nodePort:
